[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "verita.settings_test"
python_files = ["test_*.py", "*_test.py", "tests.py"]
addopts = [
    "-ra",
    "--strict-markers",
    "--tb=short",
]
markers = [
    "concurrency: tests that exercise concurrent transactions / threads",
]
